Modules used to create the app:
vite, tailwind css,react-three/fiber (for three.js in react)
other dependancies for app:
npm install --legacy-peer-deps 
@react-three/
    drei maath 
    react-tilt 
    react-vertical-timeline-component 
    @emailjs/browser 
    framer-motion 
    react-router-dom

function App(){} avoid use modern arrow function:
const App = () => {} 

li
motion.js - framer motion animations
constants folder:
    index.js - content of application which is ref everywhere

Running: npm run dev on vite server 

Steps:
1) in App.jsx 
    import browser router
    components(about, contact, hero , exp, feedback, navbar, tech, works, starscanvas)-./components


  
git config --global user.email "you@example.com"
  git config --global user.name "Your Name"


